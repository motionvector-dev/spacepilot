name: tests

on:
  push:
    branches: ["**"]
  pull_request:

# Runs on Linux because this repo is private and macOS runners bill at 10x.
# The product itself is macOS-only (videotoolbox, CoreML, MLX): no test asserts
# a successful videotoolbox render, so the suite is portable, but anything that
# starts depending on those encoders needs a macos-latest job instead.
jobs:
  pytest:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ffmpeg

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # torch/diffusers are only needed by the GPU worker, which is stubbed
          # in tests and never runs here.
          pip install fastapi uvicorn "pydantic>=2" pillow flask pytest httpx kokoro-onnx

      - name: Run tests
        run: python -m pytest tests/ -q
