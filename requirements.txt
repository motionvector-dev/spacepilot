# What the app and the test suite need. CI installs exactly this file, so a new
# import in src/ is not usable until it appears here. Keep it light: the GPU
# worker's heavy stack lives in requirements-gpu.txt and is never installed here.
#
# tests/test_dependency_declarations.py fails if this file and pyproject.toml
# disagree, or if the CI workflow goes back to hand-listing packages.

# Core Web API. starlette and python-multipart arrive with fastapi, but
# pluto/api/security.py and pluto/api/routes/assets.py import them directly, so
# they are ours to declare rather than fastapi's to keep.
fastapi>=0.100.0
starlette>=0.37
uvicorn>=0.22.0
python-multipart>=0.0.9
pydantic>=2.0
httpx>=0.24.0

# MCP server (spacepilot/pluto_mcp_server.py). Spec kept identical to pyproject.toml:
# 2.0 renamed FastMCP to MCPServer, so 1.x will not import.
mcp>=2.0,<3

# Audio. soundfile needs libsndfile1 from apt on Linux.
soundfile>=0.12.0
numpy>=1.24.0

# Voice generation. Runs in-process on the Mac; needs kokoro-v1.0.onnx and
# voices-v1.0.bin on disk (PLUTO_KOKORO_MODEL / PLUTO_KOKORO_VOICES).
kokoro-onnx

# Model and runtime registry (spacepilot/pluto/registry.py, spacepilot/pluto/runtimes.py).
# packaging parses the interpreter gates `runtimes list` prints, e.g.
# "needs Python >=3.10,<3.14; this interpreter is 3.14.7".
PyYAML>=6.0
packaging>=23.0

# Contention sampling (src/pluto/measurements.py): sums CPU% of processes
# outside our own process tree, which os.getloadavg() cannot distinguish.
psutil>=5.9.0
rich>=13.0

# Claim verifier (tools/verify_claims.py): fans a claim out to N cheap models
# across providers through one interface.
litellm>=1.98.0

# Image handling (pluto/services/image_utils.py, pluto/api/routes/generate.py)
pillow>=10.0

# Shared model cache resolution (paths.py)
huggingface_hub>=0.20

# Utilities
sentencepiece
protobuf

# ltx_worker.py imports flask at module level. The worker itself only ever runs
# on the GPU box, but tests/test_ltx_worker.py builds its Flask test client to
# assert seven auth and path-traversal refusals — without flask those tests do
# not fail, they error, and read as infrastructure noise.
flask>=3.0

# Tests
pytest
