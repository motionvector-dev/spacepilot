# Core Web API
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0

# ML and Deep Learning Frameworks
# Note: For Apple Silicon (MPS) or CUDA, install the appropriate torch binaries.
torch>=2.1.0
transformers>=4.36.0
diffusers>=0.25.0
accelerate>=0.25.0

# Utilities
pillow
huggingface_hub
sentencepiece
protobuf

# Voice generation. Runs in-process on the Mac; needs kokoro-v1.0.onnx and
# voices-v1.0.bin on disk (PLUTO_KOKORO_MODEL / PLUTO_KOKORO_VOICES).
kokoro-onnx

# GPU worker (ltx_worker.py). torch/torchao come from the DLAMI on the box.
flask

# Tests
pytest
httpx
PyYAML>=6.0
