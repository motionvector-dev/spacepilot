__pycache__/
*.mp4
*.wav
*.pyc
.env
venv/
.venv/
outputs/
scratch/
.studio_token
.pluto_config.json

# tooling / os
.DS_Store
.pytest_cache/
.ruff_cache/
*.egg-info/
.idea/
.vscode/

# local model weights cache — never committed.
# Anchored to the repo root on purpose: a bare `models/` also matches
# registry/models/, which is source, and git add skips ignored files without
# saying so — the model registry was silently absent from a commit that
# claimed to add it.
/models/

# legacy studio snapshot kept on disk only; web/ history is in git already
studio_backup_original/

# agent worktrees live here and must never be committed
.claude/worktrees/
