schema: 1
id: lens
name: Lens
family: microsoft
kind: image
summary: >
  A 3.8B dual-stream MMDiT text-to-image Turbo model built over FLUX.2
  latents, combining Microsoft Lens, an OpenAI GPT-OSS-20B text encoder, and
  Black Forest Labs' FLUX.2-dev. Hugging Face tags the repo's license as
  "other" rather than naming one of its component licenses.
homepage: https://huggingface.co/Comfy-Org/Lens

license:
  id: "other (composite: Microsoft Lens MIT + GPT-OSS-20B Apache-2.0 + FLUX.2-dev Apache-2.0)"
  spdx: null
  open_source: true
  url: https://huggingface.co/Comfy-Org/Lens
  restrictions:
    - >
      Repackages three components under their own licenses (MIT, Apache-2.0,
      Apache-2.0); each is individually permissive, but the Hub lists the
      combined repo as "other" rather than stating unified terms. Verify
      against the model card before relying on this for commercial use.

variants:
  - id: lens-turbo
    name: Lens Turbo
    params: 3.8B
    precision: bf16
    repo: Comfy-Org/Lens
    backends: [metal]
    download:
      value: 41107648787
      source: huggingface-api
      checked: "2026-08-23"
    working_set:
      value: 49329178544
      source: estimated
      note: >
        download size plus a 20% margin for the text encoder and VAE held
        resident during generation; no measured run on any machine.
    notes: >
      4-step distillation with CFG internalized — guidance and negative
      prompt are never applied. mflux aliases "lens-turbo", "lens".
